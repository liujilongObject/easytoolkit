# EasyWebToolkit

> 一个**纯前端、免上传**的在线文件处理工具站。
> 你的文件不会离开浏览器——零服务器、零账号、零追踪。

🌐 **在线体验：** https://liujilongobject.github.io/easytoolkit/

---

## 这是什么？

EasyWebToolkit 是一款运行在浏览器里的**轻量级文件处理工具集**。
所有计算都通过 WebAssembly 在你本地完成，不需要登录、不需要安装客户端、不需要把文件传到任何服务器。

**打开网页 → 拖入文件 → 处理 → 下载**，就这么简单。

---

## 它能帮你做什么？

### 🖼️ 图片工具

| 功能 | 说明 |
| ---- | ---- |
| **格式转换** | PNG / JPG / WebP 三向互转，质量参数可调 |
| **智能压缩** | 选择压缩级别，算法自动搜索最佳质量与体积平衡点。JPG 通过二分搜索 quality 参数；PNG 支持无损优化与颜色量化两种策略 |
| **裁剪** | 多手柄拖拽 + 长宽比锁定（自由 / 1:1 / 4:3 / 3:2 / 16:9 / 9:16） |
| **尺寸调整** | 像素 / 百分比双模式，长宽比锁定，多种重采样滤镜（Lanczos3 / CatmullRom 等） |

### 📄 PDF 工具（开发中）

PDF 压缩、PDF ↔ 图片互转、合并 / 拆分等能力规划中。

---

## 为什么选它？

### 🔒 真正的隐私安全

- **文件不离开浏览器**：所有处理在你本机的浏览器里完成，没有任何文件、内容或元数据被发送到服务器。
- **零账号、零跟踪**：无需注册登录，不接入第三方统计/监控 SDK。
- **可离线使用**：通过 PWA 缓存机制，加载一次后即使断网也能继续使用（规划中）。
- **代码开源可审计**：从 Rust 源码到 wasm 构建产物再到 Vue 前端，全部开源透明。

> 适合处理**机密合同、个人证件、内部资料**等任何不便上传到第三方网站的文件。

### ⚡ 高性能 · 不卡顿

- **WebAssembly 原生级速度**：图片编解码核心由 Rust 编译为 wasm，性能接近本地软件。
- **Web Worker 后台计算**：所有重活都在子线程跑，UI 永远流畅，不会让你的页面卡死。
- **路由级懒加载**：进入对应工具页才加载相关 wasm 模块，首屏极快（首屏 JS gzip 仅 ~38 KB）。

### 🎯 简单易用

- 拖拽上传、即时预览、一键下载。
- 中文界面，操作路径短，参数默认值经过精心调优。
- 转换前后体积对比一目了然。

---

## 核心技术亮点

| 维度 | 实现 |
| ---- | ---- |
| **语言 / 框架** | Vue 3 + TypeScript + Vite + UnoCSS |
| **计算核心** | Rust → WebAssembly（`image` crate / `quantizr` 等纯 Rust 库） |
| **并发模型** | Web Worker + Comlink RPC，主线程仅负责 UI 与文件 I/O |
| **类型安全** | Rust ↔ TypeScript 类型由 `wasm-bindgen` 自动生成，端到端无 `any` |
| **零拷贝传输** | `ArrayBuffer` 通过 Comlink `transfer()` 在线程间转移，避免大文件复制开销 |
| **包体积控制** | image-wasm 仅启用必需格式，release 体积 660 KB（gzip < 250 KB） |

### 性能预算

| 场景 | 实测 |
| ---- | ---- |
| 10 MB 以内单张图片转换 | < 3s |
| 首屏 JS（不含 wasm） | ~95 KB / gzip 38 KB |
| image-wasm 体积 | 660 KB（预算 < 1 MB） |

---

## 浏览器兼容性

| 浏览器 | 最低版本 |
| ------ | ------- |
| Chrome / Edge | 90+ |
| Firefox | 90+ |
| Safari | 15+ |

需要支持 WebAssembly、Web Worker、File API（现代浏览器均已默认支持）。

---

## 隐私声明

我们**承诺并已通过技术实现确保**：

1. ✅ 你上传的任何文件**不会**离开你的浏览器。
2. ✅ 站点**不收集**任何文件内容、文件名、文件元数据。
3. ✅ 站点**不接入**任何会上传文件的第三方服务（统计 / 监控 / 广告 SDK）。


如果你不放心，完全可以**断网后继续使用**——你会发现一切功能都正常工作。

---

## 反馈与共建

- 提交 issue / PR：欢迎在 GitHub 仓库参与建设
- 功能建议：欢迎告诉我们你希望增加什么工具

---

> **EasyWebToolkit — 把"在线工具"重新定义为"在浏览器里跑的本地工具"。**
